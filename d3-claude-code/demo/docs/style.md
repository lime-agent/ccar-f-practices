# 스타일 표준 (CLAUDE.md 에 `@import` 되는 모듈)

이 파일 자체는 `CLAUDE.md`가 아니다. 루트 `CLAUDE.md`의 `@docs/style.md` 한 줄로 **임포트**된다.
패키지·주제별 표준을 이렇게 분리하면, 필요한 `CLAUDE.md`에만 **선택적으로 포함**할 수 있다. (Task 3.1)

## 네이밍
- 함수: `camelCase`
- 타입/컴포넌트: `PascalCase`
- 상수: `UPPER_SNAKE_CASE`

## 포맷
- 들여쓰기 2 스페이스
