---
paths: ["api/**/*"]
---

# api/ 규약 (경로 스코프)

이 규칙은 `api/` 하위 파일을 편집할 때만 로드된다(경로 매칭). 무관한 컨텍스트·토큰 사용을 줄인다. (Task 3.3)

- 모든 엔드포인트에 입력 검증
- 에러 응답 형식 표준화(`{ error: { code, message } }`)
- 페이지네이션 없는 목록 반환 금지
